{
  "$schema": "../../gen/schemas/desktop-schema.json",
  "identifier": "settings-window",
  "description": "The Settings window may show and request the two local capture permissions, show the signed speech-model catalog, install and activate one catalog model, and remove only an inactive local model. It cannot read meetings or use generic filesystem, shell, process, dialog, remote-origin, account, or cloud authority.",
  "windows": [
    "settings"
  ],
  "platforms": [
    "macOS"
  ],
  "permissions": [
    "allow-first-run-permissions",
    "allow-first-run-request-microphone",
    "allow-first-run-request-system-audio",
    "allow-transcript-model-settings",
    "allow-install-transcript-model",
    "allow-remove-transcript-model"
  ]
}
